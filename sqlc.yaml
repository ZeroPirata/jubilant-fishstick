version: "2"
sql:
  - schema: "repository/schema.sql"
    queries: "repository/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "internal/db" 
        emit_json_tags: true
        sql_package: "pgx/v5"